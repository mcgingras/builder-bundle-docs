export const description =
  'Learn everything there is to know about the Protocol API and integrate Protocol into your product.'

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Examples', id: 'examples' },
]

# Builder Bundle

An un-opinionated, un-styled set of react hooks and components for the nouns builder ecosystem. {{ className: 'lead' }}

<div className="not-prose mb-16 mt-6 flex gap-3">
  <Button href="/quickstart" arrow="right" children="Explore the guide" />
  <Button href="/examples " variant="outline" children="Examples" />
</div>

## Getting started {{ anchor: false }}

To get started, install builder bundle using your favorite javascript package manager. Please note that builder bundle is alpha stage software, and should be treated as such. {{ className: 'lead' }}

<CodeGroup>
  ```bash {{ language: 'js' }}
  # Install builder bundle
  npm install builder-bundle
  ```
</CodeGroup>
